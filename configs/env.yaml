rom_path: "ROM/mariokart.nds"
savestate_path: "ROM/yoshi_falls_time_trial.dsv"

frames_per_step: 4
max_steps_per_episode: 2000
stuck_timeout_seconds: 5.0
per_cycle_sleep_seconds: 0.01
negative_direction_timeout_seconds: 1.0
progress_epsilon: 2

# Memory addresses (to be filled after reverse engineering / notebook work)
lap_addr: null
speed_addr: null
progress_addr: null
progress_wrap_value: null
progress_neg_delta_threshold: -2   # negative delta threshold to consider "backwards" (raw units)
negative_direction_timeout_seconds: 1.0
progress_epsilon: 2
progress_debug: false

# Additional state configuration
use_timer: false
extra_addrs: {}


